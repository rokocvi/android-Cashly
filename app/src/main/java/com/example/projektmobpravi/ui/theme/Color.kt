package com.example.projektmobpravi.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primarne boje ──────────────────────────────────────
val DeepGreen  = Color(0xFF014737)   // Premium tamno-zelena
val MintGreen  = Color(0xFF027A48)   // Brand srednja zelena
val AccentGold = Color(0xFFF79009)   // Moderni jantarni akcent

// ── Površine ───────────────────────────────────────────
val SurfaceLight = Color(0xFFF9FAFB) // Čista gotovo-bijela podloga
val SurfaceCard  = Color(0xFFFFFFFF) // Čisto bijele kartice

// ── Tekst ──────────────────────────────────────────────
val TextDark   = Color(0xFF101828)   // Gotovo crna
val TextMuted  = Color(0xFF667085)   // Srednje siva
val TextOnDark = Color(0xFFFFFFFF)   // Bijela na tamnim površinama

// ── Status ─────────────────────────────────────────────
val ErrorRed     = Color(0xFFEF4444)
val SuccessGreen = Color(0xFF12B76A)

// ── Container boje ─────────────────────────────────────
val PrimaryContainer = Color(0xFFD1FADF)  // Svjetlozeleni container za indikator

// ── Kategorije ─────────────────────────────────────────
val CategoryFood          = Color(0xFFEF4444)  // Crvena
val CategoryTransport     = Color(0xFF3B82F6)  // Plava
val CategoryEntertainment = Color(0xFF8B5CF6)  // Ljubičasta
val CategoryHealth        = Color(0xFF10B981)  // Smaragdna
val CategoryHousing       = Color(0xFFF97316)  // Narančasta
val CategoryClothing      = Color(0xFFEC4899)  // Roza
val CategoryOther         = Color(0xFF6B7280)  // Siva
